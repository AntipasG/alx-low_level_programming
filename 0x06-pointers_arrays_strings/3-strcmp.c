#include"main.h"
#include<string.h>

/**
 *_strcmp - Writeing a function that compares two strings.
 *@s1: the first parameter of the string
 *@s2: the second parameter of string
 *
 *Return: comp.
 **/

int _strcmp(char *s1, char *s2)
{
	int comp = strcmp(s1, s2);

	return (comp);
}
