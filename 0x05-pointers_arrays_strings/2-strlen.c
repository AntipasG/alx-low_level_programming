#include "main.h"
#include <string.h>

/**
 *_strlen - this is finding the string length
 *@s: this is the string address
 *
 *
 *Return: len the length
 **/

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
